services:
  - type: web
    name: studybuddy-be
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn index:app --host 0.0.0.0 --port $PORT
    envVars:
      - sync: false